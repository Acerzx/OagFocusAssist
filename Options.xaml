<ResourceDictionary x:Class="OagFocusAssist.Options"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:OagFocusAssist">

    <DataTemplate x:Key="OAG Manual Focus Assist_Options">
        <StackPanel Margin="10">

            <!-- Demo-режим: надпись + чекбокс на одной строке -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Demo-режим" 
                           FontSize="13" FontWeight="SemiBold" 
                           VerticalAlignment="Center" Margin="0,0,12,0"/>
                <CheckBox Grid.Column="1" 
                          IsChecked="{Binding DemoMode, Mode=TwoWay}"
                          VerticalAlignment="Center"/>
            </Grid>
            <TextBlock Text="Генерация синтетической V-кривой HFR. Отключите для работы с реальной камерой."
                       TextWrapping="Wrap" Margin="0,0,0,15" FontSize="11" Opacity="0.7"/>

            <Separator Margin="0,0,0,15"/>

            <!-- Экспозиция по умолчанию: надпись + поле ввода на одной строке -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Экспозиция по умолчанию:" 
                           FontSize="13" FontWeight="SemiBold" 
                           VerticalAlignment="Center" Margin="0,0,12,0"/>
                <TextBox Grid.Column="1" 
                         Text="{Binding DefaultExposure, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:F2}}"
                         Width="80" Padding="6,4" FontSize="13" 
                         VerticalAlignment="Center" HorizontalContentAlignment="Center"/>
            </Grid>
            <TextBlock Text="Рекомендуемое значение для OAG: 0.1–1.0 сек."
                       Margin="0,0,0,0" FontSize="11" Opacity="0.7"/>

        </StackPanel>
    </DataTemplate>
</ResourceDictionary>